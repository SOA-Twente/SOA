spring.config.import=file:DirectMessageApp/.env[.properties]

spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/profiledatabase
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}
spring.jpa.show-sql=true

server.port=8754

service-profile-url=http://test.url
service-postmessage-url=http://test.url/getQuacksByUserId/{id}

active-mq.create-conversation-queue=
active-mq.get-conversation-queue=
active-mq.send-msg-queue=